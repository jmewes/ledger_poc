<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow the JVM to use JIT compilation under hardened runtime -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (required by the JVM) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Disable library validation to allow loading JARs and native libs -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Full outbound network access -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Full inbound network access -->
    <key>com.apple.security.network.server</key>
    <true/>
</dict>
</plist>
